# Third-party code and data in Shockmate

| What | Where | Licence | Notes |
| --- | --- | --- | --- |
| Stockfish (WASM build by Niklas Fiekas, stockfish.js 10.0.2, single-thread) | `web/vendor/stockfish.wasm`, `web/vendor/stockfish.wasm.js` | GPL-3.0, text in `web/vendor/stockfish-COPYING.txt` | Runs in a worker for Play, Versus and the silent post-game review. Unmodified. |
| Stockfish 19 (native) | not in the repo | GPL-3.0 | Used only by `tools/build_encounters.py` and `tools/build_ladder.py` on the author's PC to score fights. Its output is data. |
| chess.js 1.0.0-beta.8 (Jeff Hlywa) | `web/vendor/chess.js` | BSD-2-Clause, text in `web/vendor/chess.js-LICENSE.txt` | Wrapped for a plain script tag; the source between the markers is unmodified. |
| python-chess | pip, not in the repo | GPL-3.0 | Build scripts and data tests only. |
| Lichess puzzle database | not in the repo (`~/tools/lichess/`) | CC0 | Source of the 120 ladder fights in `data/ladder.src.json`. |
| Baloo 2, Atkinson Hyperlegible, Patrick Hand | loaded from Google Fonts | SIL Open Font License 1.1 | Cached by the service worker after first load. |
| Voice lines | `web/voice/*.mp3` | generated with ElevenLabs under the author's account | Regenerated by `tools/voice/generate.py`; the key is never in the repo. |
| Playwright, greenlet | pip, tests only | Apache-2.0, MIT | `tests/e2e/`. |

Shockmate's own code (`web/*.js` except `vendor/`, `tools/`, `tests/`, `data/`) has no licence file yet;
that choice belongs to the repository owner. Shipping the GPL-3 Stockfish build means any licence chosen
for a combined distribution has to be GPL-compatible.
